# 디버깅, 람다식, 함수

## 1️⃣ 디버깅

- 디버깅은 **소프트웨어의 오류를 식별하고 수정하는 과정**이다.

### 디버깅의 중요성

- 디버깅은 소프트웨어가 **올바르게 작동하는지 확인**하는 데 필수적이다.
- 디버깅을 하지 않으면
    - 소프트웨어에 오류가 발생하거나
    - 겉으로는 실행되지만 제대로 동작하지 않을 수 있다.

### 디버깅 도구와 기법

- **로깅**
    - 코드가 실행되는 동안 발생하는 이벤트를 기록한다.
    - 어느 시점에 어떤 값이 들어왔는지, 어떤 분기가 실행됐는지 추적할 수 있다.
- **브레이크포인트**
    - 특정 줄에서 프로그램 실행을 일시 중지한다.
    - 중단 시점에 변수 값, 호출 스택 등을 확인할 수 있다.
    - 브레이크포인트는 **그 줄을 실행하기 직전**에 동작한다.
- **디버거**
    - 브레이크포인트, 변수 관찰, 스텝 실행(step over, step into) 등을 제공하는 도구이다.
    - IDE에 내장된 디버거를 사용하면 디버깅을 훨씬 쉽게 할 수 있다.
- **스택 추적(Stack Trace)**
    - 예외 발생 시, 어떤 함수들이 어떤 순서로 호출되다가 오류가 발생했는지 알려준다.
    - 버그의 위치와 호출 경로를 파악하는 데 매우 유용하다.

### 디버깅 팁

- **작게 시작하기**
    - 디버깅할 때는 작은 문제, 좁은 범위부터 시작하는 것이 좋다.
    - 한 번에 한 가지 문제에 집중할 수 있어 원인 파악이 쉬워진다.
- **단순하게 유지하기**
    - 디버깅 중에는 코드와 상황을 가능한 한 단순하게 유지한다.
    - 조건·분기·상태가 복잡할수록 버그 원인을 찾기 어렵다.
- **인내심 갖기**
    - 디버깅은 시간이 오래 걸릴 수 있으므로 조급해하지 않는다.
    - 오류를 바로 못 찾아도, 단서를 하나씩 모은다는 느낌으로 접근한다.

---

## 2️⃣ 람다식과 함수

### 일급 객체 (first class object)

- 변수에 대입하거나, 함수의 인자로 전달하거나, 반환값으로 사용할 수 있는 대상을 말한다.
- 대표적인 일급 객체
    - 값(숫자, 문자열 등)
    - 인스턴스(객체)
    - 함수
- 코틀린에서는 **함수도 일급 객체**이므로, 함수를 값처럼 다룰 수 있다.

### 함수 표현 방법

- **일반 함수**
    - 블록 안에 로직을 작성하고, `return` 으로 결과를 돌려준다.
- **식 본문 함수 (expression body, single expression function)**
    - 함수 본문이 식 하나일 때, 블록 없이 `=` 오른쪽에 식만 적는다.
- **람다식 (lambda expression)**
    - 이름 없는 함수 표현.
    - 변수에 저장하거나, 다른 함수의 인자로 넘길 때 자주 사용한다.
- **후행 람다 (trailing lambda)**
    - 함수의 마지막 인자가 람다인 경우, 함수 호출의 소괄호 밖에 람다를 작성할 수 있다.
- **단일 매개변수 람다와 `it`**
    - 매개변수가 하나뿐이면 매개변수 이름을 생략하고 `it` 으로 접근할 수 있다.

```kotlin
val sumLambda: (Int, Int) -> Int = { a, b -> a + b }

// 단일 매개변수 람다에서 it 사용
val square: (Int) -> Int = { it * it }

// 후행 람다 예제
repeat(3) {
    println("반복")
}
```

---

### 함수 참조 (function reference)

- 이미 정의된 함수를 값으로 넘기고 싶을 때, 함수 이름 앞에 `::` 를 붙여 사용한다.
- 입력 타입과 출력 타입이 일치하면, 같은 타입의 값으로 취급할 수 있다.
- 이 방식을 통해 함수를 다른 함수의 인자나 반환값으로 사용할 수 있다.  
  → 함수가 **일급 객체**로 취급된다는 의미.

```kotlin
// 함수를 값으로 전달하는 예 (function reference)
fun printElement(element: Int) {
    println(element)
}

fun main() {
    val list = listOf(1, 2, 3)

    // 함수 참조를 사용해 forEach에 전달
    list.forEach(::printElement)
}
```

### 람다식과 단일 메서드 인터페이스

- 메서드가 하나만 있는 인터페이스(함수형 인터페이스)는 람다식으로 대체해 사용할 수 있다.
- 예를 들어, 정렬 시 `Comparator` 구현 대신 람다식을 전달할 수 있다.

```kotlin
// 단일 메서드 인터페이스 자리에 람다 사용 예
val list = listOf(3, 1, 2)

// Comparator 대신 람다식을 전달
val sorted = list.sortedWith { o1, o2 -> o1 - o2 }
println(sorted) // [1, 2, 3]
```

---

## 3️⃣ 함수형 프로그래밍

- 자료 처리를 **수학적 함수의 계산**으로 바라보는 프로그래밍 패러다임.
- 특징
    - 가변 데이터와 상태 변경을 최소화한다.
    - 부수 효과(side effect)를 줄이고, 같은 입력에 항상 같은 출력이 나오도록 하는 것을 선호한다.
    - 코드가 선언적이며, 유지보수성·가독성이 좋다.

### 고계 함수 (higher-order function)

- **함수를 다루는 함수**.
- 구체적으로,
    - 함수를 **파라미터로 받거나**
    - 함수를 **결과로 반환하는** 함수를 말한다.
- 1차 함수
    - 숫자, 문자열 같은 일반적인 데이터를 인자로 받는 함수. (개념적으로 `f: A → B`)
- 고계 함수
    - **함수를 데이터처럼 인자로 받는** 함수. (개념적으로 `G: (A → B) → C`)

### Kotlin에서 제공되는 대표적인 고계 함수

- `filter` : 조건에 맞는 요소만 필터링한다.
- `map` : 각 요소를 다른 값으로 **변환**해 새 컬렉션을 만든다.
- `forEach` : 컬렉션의 모든 요소를 순회하며 주어진 작업을 수행한다.
- `reduce` : 컬렉션의 요소를 하나씩 줄여 가며 하나의 값으로 만든다.
- `fold` : 초기값을 가진 `reduce` 처럼, 누적값을 접어 나가며 결과를 만든다.
- `any` : 조건을 만족하는 요소가 하나라도 있는지 확인한다.

```kotlin
val numbers = listOf(1, 2, 3, 4, 5)

val evens = numbers.filter { it % 2 == 0 }             // [2, 4]
val doubled = numbers.map { it * 2 }                   // [2, 4, 6, 8, 10]
val sum = numbers.reduce { acc, i -> acc + i }         // 15
val sumWithFold = numbers.fold(0) { acc, i -> acc + i } // 15
val hasEven = numbers.any { it % 2 == 0 }              // true
```

### 주의 사항과 특징

- `toSet()` 처럼 함수 인자를 받지 않는 단순 변환 함수는 보통 고계 함수라고 부르지 않는다.
- `forEach`, `map` 등 대부분의 고계 함수는 **전체를 순회**한다.
    - `forEach` 안에서는 중간에 `break` 로 루프를 끊을 수 없다.
    - 필요하다면 일반 `for` 문,
      혹은 `any`, `firstOrNull` 같은 함수를 사용해 조건을 만족하는 순간 멈추도록 설계하는 편이 낫다.
- 함수형 프로그래밍 스타일은 일반 루프보다 성능은 조금 떨어질 수 있으나,
    - 코드가 더 선언적이고 읽기 쉬워
    - 유지보수성과 가독성이 좋다는 장점이 있다.

---

## 📘 알아두면 좋은 내용

- 테스트 코드 작성 시 실행 전·후 공통 작업을 분리하는 것이 좋다.
- `@Before`
    - 각 테스트 메서드가 실행되기 전에 한 번씩 실행된다.
    - 공통 초기화 작업(객체 생성, 공용 리소스 준비 등)에 사용한다.
- `@After`
    - 각 테스트 메서드가 실행된 후에 한 번씩 실행된다.
    - 리소스 해제, 임시 파일 삭제 등 정리 작업에 사용한다.

---

## 과제 리뷰

> ### 연습문제 1
>
> 파일에서 json 데이터를 읽어서 처리해야 한다.
> 이 데이터를 담을 수 있는 클래스를 작성하시오

```kotlin
@Serializable
data class SalePrice(
    val price: Double? = null,
    val cvtDatetime: LocalDateTime? = null
)

@Serializable
data class ChartData(
    val collectionName: String = "",
    val collectionSalePrice: List<SalePrice> = emptyList()
)

@Serializable
data class ChartDataList(
    val collectionChartDataList: List<ChartData> = emptyList()
)
```

- 주어진 정보가 계층구조 되어있어서 역직렬화한 정보를 저장하기위해 계층구조로 정의했다.

```kotlin
@Serializable
abstract class ChartSerialize {
    companion object {
        fun obj(source: File): ChartDataList =
            Json.decodeFromString<ChartDataList>(source.readText())
    }
}
```

- 주어진 JSON 파일을 읽어서 위에서 만든 구조로 저장하기위한 함수이다.

---

> ### 연습문제 2
>
>  1. 2011년에 일어난 모든 트랜잭션을 찾아 가격 기준 오름차순으로 정리하여 이름을 나열하시오
>  2. 거래자가 근무하는 모든 도시를 중복 없이 나열하시오
>  3. 케임브리지에서 근무하는 모든 거래자를 찾아서 이름순으로 정렬하여 나열하시오
>  4. 모든 거래자의 이름을 알파벳순으로 정렬하여 나열하시오
>  5. 밀라노에 거래자가 있는가?
>  6. 케임브리지에 거주하는 거래자의 모든 트랙잭션값을 출력하시오
>  7. 전체 트랜잭션 중 최대값은 얼마인가?
>  8. 전체 트랜잭션 중 최소값은 얼마인가?

```kotlin
val transactions = listOf(
    Transaction(Trader("Brian", "Cambridge"), 2011, 300),
    Transaction(Trader("Raoul", "Cambridge"), 2012, 1000),
    Transaction(Trader("Raoul", "Cambridge"), 2011, 400),
    Transaction(Trader("Mario", "Milan"), 2012, 710),
    Transaction(Trader("Mario", "Milan"), 2012, 700),
    Transaction(Trader("Alan", "Cambridge"), 2012, 950)
)

// 1. 2011년에 일어난 모든 트랜잭션을 찾아 가격 기준 오름차순으로 정리하여 이름을 나열하시오
transactions.filter { it.year == 2011 }
    .sortedBy { it.value }
    .map { it.trader.name }
    .forEach { println(it) }

// 2. 거래자가 근무하는 모든 도시를 중복 없이 나열하시오
transactions.map { it.trader.city }
    .toSet()
    .forEach { println(it) }

// 3. 케임브리지에서 근무하는 모든 거래자를 찾아서 이름순으로 정렬하여 나열하시오
transactions.filter { it.trader.city == "Cambridge" }
    .sortedBy { it.trader.name }
    .map { it.trader.name }
    .toSet()
    .forEach { println(it) }

// 4. 모든 거래자의 이름을 알파벳순으로 정렬하여 나열하시오
transactions.sortedBy { it.trader.name }
    .map { it.trader.name }
    .toSet()
    .forEach { println(it) }

// 5. 밀라노에 거래자가 있는가?
println(transactions.any { it.trader.city == "Milan" })

// 6. 케임브리지에 거주하는 거래자의 모든 트랙잭션값을 출력하시오
transactions.map{it.value}
    .forEach { println(it) }

// 7. 전체 트랜잭션 중 최대값은 얼마인가?
println(transactions.maxOf { it.value })

// 8. 전체 트랜잭션 중 최소값은 얼마인가?
println(transactions.minOf { it.value })
```