# 인스턴스 기본 조작

---

## 1️⃣ Object / Any 클래스

- 자바에서 모든 클래스는 `Object` 클래스를 기본으로 상속한다.
- 코틀린에서는 `Any`가 최상위 클래스지만, 실제 구현은 `Object` 기반.

### 대표 메서드

| 메서드 | 설명 |
|---------|------|
| `toString()` | 객체의 문자열 표현 (디버깅용) |
| `equals()` | 객체의 내용 비교 (`==`과 동일) |
| `hashCode()` | 객체의 해시값 반환 |

---

## 2️⃣ equals()와 === 차이

| 구분 | 의미 | 예시 |
|------|------|------|
| `==` | **내용 비교** (equals() 호출) | `a == b` |
| `===` | **참조(주소) 비교** | `a === b` |

---

## 3️⃣ 컬렉션

| 컬렉션 | 비교 기준 |
|---------|------------|
| `List` | `equals()` (내용 비교) |
| `Set`, `Map` | `hashCode()` (빠른 검색) |

### 해시 관련 주의
- 모든 객체는 해시값을 가진다.
- **동일한 객체는 항상 같은 해시값**을 가진다.
- 그러나 **같은 해시값이더라도 동일 객체는 아닐 수 있음**.
- **equals()와 hashCode()는 함께 재정의**해야 일관성을 유지할 수 있다.

---

## 4️⃣ 정렬 (Sorting)

Kotlin에서는 컬렉션(`List`)을 정렬할 때 여러 가지 방식을 제공한다.  
기본적으로 정렬 결과는 **새로운 리스트**로 반환되며, **원본 리스트는 변경되지 않는다.**

---

### 기본 정렬: `sorted()`

- `sorted()`는 리스트의 요소가 `Comparable` 인터페이스를 구현하고 있어야 작동한다.
- 문자열(`String`)이나 숫자(`Int`, `Double`)처럼 기본 타입은 이미 `Comparable`을 구현하고 있다.
- **복사본**은 만들어서 정렬

> **정리**
> - `sorted()`는 오름차순 정렬
> - 내부적으로 `compareTo()` 메서드를 사용
> - 원본 리스트(`names`)는 변경되지 않음

---
### 직접 정렬 기준 지정: `Comparator`

- `Comparator`는 정렬 규칙을 **즉석에서 지정할 때** 사용한다.
- `sortedBy {}`는 객체 리스트를 특정 속성 기준으로 정렬할 때 사용한다.

---

### 클래스 내부에서 정렬 기준 정의: `Comparable`

- 클래스 자체에 `Comparable`을 구현하면 `sorted()`에서 자동으로 그 기준을 따른다.
- `compareTo()` 메서드에서 기준을 정하면 된다.
- 역순으로 정렬하려면 부호를 반대로 반환하면 된다.

---

## 5️⃣ 복사 (Copy)

Kotlin에서는 객체를 복사할 때 **얕은 복사(Shallow Copy)** 와 **깊은 복사(Deep Copy)** 의 개념이 중요하다.  
특히 `data class`의 `copy()` 메서드는 **기본적으로 얕은 복사**를 수행한다.

---

### 얕은 복사 (Shallow Copy)

> 객체의 "참조"만 복사하는 방식으로, 내부 객체는 **같은 주소**를 공유한다.

```kotlin
class Address(
    var street: String
)

class Person(
    val name: String,
    val age: Int,
    val address: Address
) {
    fun shallowCopy() = Person(name, age, address)
}
```

- 같은 메모리 주소를 가리키기 때문에 한쪽을 수정하면 다른 쪽도 영향을 받는다.
- 메모리 사용은 적지만 **부작용(side effect)** 이 발생할 수 있다.

---

### 깊은 복사 (Deep Copy)

> 내부에 포함된 객체까지 새로 복제하는 복사 방식이다. 
> 완전히 독립된 객체로, 한쪽을 변경해도 다른 쪽은 영향을 받지 않는다.

```kotlin
class Address(
    var street: String
) {
    fun deepCopy() = Address(street)
}

class Person(
    val name: String,
    val age: Int,
    val address: Address
) {
    fun deepCopy() = Person(name, age, address.deepCopy())
}
```

- 리스트나 객체 등 **참조 타입**을 복제할 때는 내부까지 새로 만들어야 진짜 깊은 복사이다.
- 직접 `deepCopy()` 메서드를 만들어 내부 필드까지 복제하도록 구현할 수 있다.

---

### data class

- **Kotlin**의 `data class`는 자동으로 **copy()** 메서드를 제공하고, 이는 얕은 복사이다.

---

## 📘 알아두면 좋은 내용

- **인터페이스는 많이 사용할수록 객체지향적 설계가 된다.**
- **수정에 용이함** → 추상클래스는 단일 상속만 가능하지만, 인터페이스는 다중 구현 가능.
- **탑다운(Top-down)** 보다 **바텀업(Bottom-up)** 설계가 더 쉽다.  
  → 먼저 하위 클래스를 만들고, 공통 속성은 추상클래스로, 공통 동작은 인터페이스로 묶어 나간다.

| 기능 | 단축키 |
|------|--------|
| 클래스의 모든 메서드 확인 | `Ctrl + F12` |
| 상속된 모든 클래스 확인 | `Ctrl + H` |

---

## 과제 리뷰

> ### 연습문제 1
> 
> 다음 동작을 할 수 있도록 Book 클래스를 수정하시오
> 
> 1) 제목과 출간일(2024-01-01)이 같으면 같은 책으로 판단한다
> 2) Book 인스턴스를 담고 있는 컬렉션에 대해 sorted() 를 수행하면 출간일이 신상 순서대로 정렬되어야 함
> 3) 깊은 복사를 제공한다

```kotlin
override fun equals(other: Any?): Boolean {
    if (this === other) return true
    if (javaClass != other?.javaClass) return false

    other as Book

    if (title != other.title) return false
//  if (author != other.author) return false  // 제목과 출간일만 같아야 하므로 작가는 제외
    if (publishedDate != other.publishedDate) return false

    return true
}
```

- `equals()`를 재정의 하였고, `author`속성만 검사를 제외하여 `title`과 `publishedDate`가 같을때만 같다고 본다.

---

```kotlin
val books = listOf(
    Book("날개", "이상", PublishedDate(LocalDate.of(1936, 9, 3))),
    Book("운수 좋은 날", "현진건", PublishedDate(LocalDate.of(1924, 6, 11))),
    Book("무정", "이광수", PublishedDate(LocalDate.of(1917, 1, 1)))
)

val results = books.sortedBy { it.publishedDate.publishedLocalDate }
```

- `sortedBy()`로 책의 날짜만 비교한다.

---

```kotlin
class PublishedDate(
    val publishedLocalDate: LocalDate = LocalDate.now()
) {
    fun deepCopy() = PublishedDate(publishedLocalDate)
}

class Book(
    val title: String,
    val author: String,
    val publishedDate: PublishedDate,
) {
    fun deepCopy() = Book(title, author, publishedDate.deepCopy())
}
```

- `publishedDate`의 타입을 `LocalDate`가 아닌 클래스로 객체화하여 깊은 복사를 구현했다.
