# 🧠 Kotlin 객체지향 및 상속 정리

---

## ⚙️ init 블록과 getter/setter의 시점 차이

- **`init` 블록** → 객체가 **생성될 때 한 번만 실행**
- **`getter` / `setter`** → 프로퍼티에 **접근하거나 변경할 때마다 실행**
- 즉, 실행 시점이 다르며 용도도 다르다.

---

## 💡 Double(실수) 연산과 엡실론 문제

- `0.1 + 0.2 == 0.3` → `false`일 수 있음
- 이유: **부동소수점 오차** 때문
- `Double`이나 `Float`을 사용하면 원하는 결과가 안 나올 수 있다.

---

## 🧩 상속(Inheritance)

### 상속의 필요성

- “이전에 만든 클래스와 닮았지만, 일부 다른 클래스”를 만들 필요가 있을 경우가 많다.
- 복사, 붙여넣기의 문제점
    - 추가, 수정에 시간이 걸림
    - 소스의 파악이나 관리가 어려워짐
- 해결책으로 **상속**을 활용한다.

---

### 자바와 코틀린의 차이

- 자바는 기본적으로 상속을 허용하며, 금지 시 `final` 사용.
- 코틀린은 기본적으로 상속을 금지하며, 허용 시 `open` 사용.

---

### 다중상속

- 코틀린에서 다중상속은 객체지향과 맞지 않아 불가능하다.
- 다중상속은 개념적으로 불명확하고 구조적 충돌이 생길 수 있다.

---

### 재정의(override)

- `open`을 해야지만 재정의(override)가 가능하다.
- 부모 객체 참조 시 `super`를 사용한다.

---

### 인스턴스화

- 인스턴스화가 가능해야 클래스를 만들 수 있다.
- 예시: `손흥민`은 인스턴스화 가능하지만, 추상적인 개념은 불가능하다.

---

## 🧩 올바른 상속 — is-a 원칙

- 올바른 상속은 “is-a 원칙”을 따른다.
- 예: `SuperHero is a Hero` → `SuperHero`는 `Hero`의 한 종류이다.

---

## ❌ 잘못된 상속의 예시

- 현실 세계의 등장인물 사이에 개념적으로 is-a 관계가 되지 않음에도 상속을 사용하는 경우가 있다.
- 예) 필드로 이름과 가격을 가지는 `Item` 클래스 (약초, 포션)를 상속받는 `House` 클래스 → 잘못된 상속.

---

## 🚨 잘못된 상속의 문제점

- 클래스를 확장할 때 현실 세계와의 모순이 생긴다.
- 객체 지향의 3대 특징 중 하나인 다형성(polymorphism)을 이용할 수 없게 된다.
- 예: 포션은 무기가 아니다 → 개념적으로 잘못된 상속.

---

## 구체화와 일반화

- 자식 클래스일수록 **구체화**되고,  
  부모 클래스일수록 **추상적**이며 일반화된다.

---

## 생성자 동작

- 다중 구조의 인스턴스가 생성될 때, 자동적으로 **가장 외측 인스턴스의 생성자**가 호출된다.
- 모든 생성자는 “부모 인스턴스의 생성자”를 호출할 필요가 있다.

---

## 과제 리뷰

> ### 연습문제 1
>
> 다음 중에서 “잘못 된 상속" 인 것을 모두 구하시오
>
> | 번호 | 슈퍼클래스 | 서브클래스 |
> |------|-------------|-------|
> | 1 | Person | Student |
> | 2 | Car | Engine |
> | 3 | Father | Child |
> | 4 | Food | Susi |
> | 5 | SuperMan | Man |

- "`서브클래스`는 `슈퍼클래스`이다." 가 성립하면 참이다.
- 따라서 정답은 1번, 4번

---

> ### 연습문제 2
>
> 다음 클래스에 대해 “부모 클래스" 와 “자식 클래스" 를 1개씩 생각 해 보시오
> 
> 1) Phone
> 2) Car
> 3) Dictionary

1) 전자기기, 갤럭시 폴드
2) 이동수단, 소나타
3) 책, 영어사전

---

> ### 연습문제 3
>
> 이 클래스를 이용해, 다음 사양을 따르는 `PoisonSlime` 클래스를 작성하시오.
>
> 1. **독 슬라임(PoisonSlime)** 은, 슬라임(Slime) 중에서도 특히 “독 공격”이 되는 것이다.
>
> 2. `PoisonSlime`는 아래의 코드로 인스턴스화 되는 클래스이다.
>
>   ```kotlin
>   val poisonSlime = PoisonSlime("A")
>   ```
>
> 3. `PoisonSlime`은 독 공격 가능 횟수를 저장하는 `poisonCount`(초기값 5)를 가진다.
> 4. `PoisonSlime`의 `attack()` 메서드가 호출되면 다음 동작을 수행한다.  
     >    a. 우선, “보통 슬라임과 같은 공격”을 한다.  
     >    b. `poisonCount`가 0이 아니면 다음을 추가로 수행한다.  
     >    c. 화면에 `"추가로, 독 포자를 살포했다!"`를 표시한다.  
     >    d. 용사의 최대 HP의 1/5에 해당하는 포인트를 용사의 HP로부터 감소시키고,  
     >       `"~포인트의 데미지"`라고 표시한다.  
     >    e. `poisonCount`를 1 감소시킨다.  

```kotlin
class PoisonSlime(
    poisonCount: Int = DEFAULT_POISON_COUNT
) : Slime("PoisonSlime") {
    // poisonCount를 내부에서만 변경
    private var _poisonCount = poisonCount

    val poisonCount: Int
        get() = _poisonCount
```

- 매직넘버를 줄이기 위해 기본값은 `companion object`에 저장
- **poisonCount**는 내부에서만 변경이 가능하고 `getter`를 통해 값은 확인 가능하다.

```kotlin
// attack() 재정의
override fun attack(hero: Hero) {
    val poisonDamage = hero.maxHp / 5 // 최대체력의 1/5

    // 보통 슬라임 공격
    super.attack(hero)

    // poisonCount가 남아있다면 추가 공격
    if (_poisonCount <= 0) {
        return
    }
    println("추가로, 독 포자를 살포했다!")
    hero.hp -= poisonDamage
    println("${poisonDamage}포인트의 데미지")
    _poisonCount--
}
```

- `attack()`을 재정의 하여 특수한 상황에서만 재정의한 `attack()`이 작동하게 설계했다.

---

> ### 연습문제 4
> 
> **Wizard 클래스 수정**
> 
> - 속성:
>   - mp: Int (초기값 100)
> - 메서드:
>   - heal(hero: Hero): Unit의 hp를 20 회복시키고 자신의 mp를 10 소모
>   - mp가 부족하면 "마나가 부족합니다" 출력
>   - 힐을 성공하면 "힐을 시전했습니다. 대상 HP: ${hero.hp}" 출력

```kotlin
companion object {
    const val DEFAULT_MP = 100
}
```

- `companion object`에 초기값 100 설정

```kotlin
fun canHeal(hero: Hero, cost: Int): Boolean {
    // 마나가 부족할 경우
    if (mp < cost) {
        println("마나가 부족합니다")
        return false
    }

    // hp가 이미 최대일 경우
    if (hero.hp == hero.maxHp) {
        println("이미 최대체력입니다")
        return false
    }
    return true
}
```

- 힐 가능 여부를 확인하는 메서드
- `Boolean`형으로 참, 거짓을 반환한다.

```kotlin
open fun heal(hero: Hero) {
    val healAmount = 20
    val healCost = 10

    if (!canHeal(hero, healCost)) return

    // 힐량이 최대체력보다 많을 경우
    if (hero.hp + healAmount > hero.maxHp) hero.hp = hero.maxHp else hero.hp += healAmount

    mp -= healCost
    println("힐을 시전했습니다. 대상 HP: ${hero.hp}")
}
```

- `open`을 활용하여 자식클래스에서도 사용가능
- `canHeal()`을 활용하여 힐 가능여부 확인

---

> ### 연습문제 5
> - GreatWizard 클래스 요구사항:
>   - Wizard 클래스를 상속받음
> - 속성:
>   - mp (초기값 150)
> - 메서드:
>   - heal(hero: Hero) 재정의 :  hp를 25 회복시키고 자신의 mp를 5 소모
>   - superHeal(hero: Hero): Unit의 hp를 전부 회복시키고 자신의 mp를 50 소모
>   - mp가 부족하면 "마나가 부족합니다" 출력
>   - 힐을 성공하면 "슈퍼 힐을 시전했습니다. 대상 HP: ${hero.hp}" 출력

```kotlin
class GreatWizard(
    name: String,
    hp: Int,
) : Wizard(name, hp, DEFAULT_GREAT_WIZARD_MP)
```

- 부모 클래스 생성자 호출
- mp의 초기값을 150으로 설정하여 `companion object`에 저장

```kotlin
// heal() 재정의
override fun heal(hero: Hero) {
    val greatHealAmount = 25    // Great Wizard의 힐량
    val greatHealCost = 5       // Great Wizard의 힐코스트

    if (!canHeal(hero, greatHealCost)) return

    // 힐량이 최대체력보다 많을 경우
    if (hero.hp + greatHealAmount > hero.maxHp) hero.hp = hero.maxHp else hero.hp += greatHealAmount

    mp -= greatHealCost
}
```

- 부모 클래스의 `heal()`을 재정의
- 부모 클래스의 `canHeal()`을 사용하여 필요한 경우에만 재정의된 `heal()`이 작동한다.

