# Kotlin - 추상클래스와 인터페이스 정리

---

## 🧱 추상 클래스 (abstract class)

- 상속의 **재료**로 사용되는 클래스
- 인스턴스화(객체 생성) **불가능**
- 내부에 **추상 메서드(abstract fun)** 를 가질 수 있음
- 추상 메서드는 **구현부 `{}` 가 없음**
- **오버라이드를 강제**하기 위해 사용
- **open 키워드 없이도 상속 가능**
- UML 다이어그램에서는 *이탤릭체(기울임체)* 로 표시됨

---

## ⚙️ 인터페이스 (interface)

- 클래스가 **아님** → **인스턴스화 불가능**
- **생성자 괄호() 없음**
- 모든 메서드는 **기본적으로 추상 메서드**
- **필드(상태)** 를 가질 수 없음
- 대신 `val`, `var` 프로퍼티는 **getter/setter 메서드**로 간주됨
- 여러 인터페이스를 **복수로 구현 가능**
    - 즉, **다중 상속 효과**를 낼 수 있음
    - 하지만 이것을 "다중 상속"이라고 부르진 않음
- 클래스는 `implements` 관계로 인터페이스를 구현

### ✳️ 인터페이스의 등장 이유
- **클래스 상속은 단일 상속만 가능**
- 하지만 인터페이스는 여러 개 **동시에 구현 가능**

---

## 🧩 추상 클래스 vs 인터페이스

| 구분 | 추상 클래스 | 인터페이스 |
|----|--------------|-------------|
| 목적 | 상속의 재료 | 역할/기능의 규약 |
| 인스턴스화 | 불가능 | 불가능 |
| 생성자 | 있음 | 없음 |
| 필드 | 가질 수 있음 | 가질 수 없음 |
| 상속 개수 | 1개만 가능 (단일 상속) | 여러 개 구현 가능 |
| 키워드 | `abstract class` | `interface` |
| 다이어그램 표기 | *이탤릭체 클래스명* | `implements` 관계 |

---

## 🔐 접근 제한자

| 키워드 | 접근 범위 | 자식 클래스 접근 가능 여부 |
|--------|------------|--------------------------|
| **private** | 같은 클래스 내부에서만 | ❌ 불가능 |
| **protected** | 같은 클래스 + 자식 클래스 | ✅ 가능 |
| **public** (기본값) | 모든 곳 | ✅ 가능 |

---

## 과제 리뷰

> ### 연습문제 1
>
> 어떤 회사에서 자산관리 프로그램을 만들려고 한다. 현시점에서 컴퓨터, 책 을 표현하는, 다음과 같은 두개의 클래스가 있다
>
> 이후, 컴퓨터와 책 이외에도 여러가지 자산을 관리하고 싶은 경우에 유용한 유형자산(TangibleAsset) 이라는 이름의 추상클래스를 작성 하시오.
> 또, Computer 나 Book 은 그 부모 클래스를 활용한 형태로 수정 하시오.

```kotlin
abstract class TangibleAsset(
    var name: String,
    var price: Int,
    var color: String,
) {
}
```
```kotlin
class Book(
    name: String,
    price: Int,
    color: String,
    var isbn: String
) : TangibleAsset(name, price, color) {
}
```
```kotlin
class Computer(
    name: String,
    price: Int,
    color: String,
    var makerName: String
) : TangibleAsset(name, price, color) {
}
```

---

> ### 연습문제 2
>
> 문제 1 의 회사에서, 형태가 없는 무형자산(IntangibleAsset) 도 관리하려고 생각하고 있다.
> 
> 무형자산에는, 예를들어 특허권(Patent) 등이 있다.
> 
> 무형자산도 유형자산도 자산(Asset)의 일종이다.
> 
> 이것을 전제로 다음의 상속도의 가, 나, 다 부분의 클래스명을 생각 해 보시오.
>
> 또한, (가) 에 들어가는 추상 클래스를 개발하고, 이 클래스를 상속하도록 TangibleAsset 를 수정하시오.
>
> 가) 자산(Asset)
> 
> 나) 무형자산(IntangibleAsset)
> 
> 다) 특허권(Patent)

---

> ### 연습문제 3
>
> 자산인지 아닌지 따지지 말고, 형태가 있는 것 (Thing) 이면, 무게가 있다
> 
> 그래서, Double 형으로 무게(weight)를 얻을 수 있도록 getter/setter (property)를 가지는 인터페이스 Thing 을 만드시오

```kotlin
interface Thing {
    var weight: Double  // 무게
}
```

- `var`는 기본적으로 `getter, setter`를 제공하기 때문에 **weight**는 메서드로 간주된다.

---

> ### 연습문제 4
>
> 유형자산 (TangibleAsset) 은, 자산 (Asset) 의 일종이며, 형태가 있는 것 (Thing) 의 일종이기도 하다.
> 
> 이 정의에 맞도록 TangibleAsset 의 소스 코드를 수정하시오.
> 
> 이 때, TangibleAsset 에 필드나 메소드의 추가가 필요하다면, 적당히 추가하시오.

```kotlin
abstract class TangibleAsset(
    name: String,
    price: Int,
    var color: String,
    override var weight: Double = 0.0
) : Asset(name, price), Thing {
}
```

- `Asset`을 상속받고, `Thing`을 구현한다.