# 예외, 파일 조작, 여러가지 데이터 형식

1. [코드 리뷰](#-코드-리뷰)
2. [예외 Exception](#예외-exception)
3. [파일 조작](#파일-조작)
4. [여러가지 데이터 형식](#데이터-형식)

## 📌 코드 리뷰

- 리터럴: “문자열” 10.0F와 같은 표현 방식

### 정규 표현식 Regular Expression

> 특정한 규칙을 가진 문자열의 집합을 표현하는데 사용되는 형식 언어

- 점 `.` : 임의의 한 문자
- 별 `*` : 앞 문자 0회 이상 반복
- 중괄호 `{}` : 지정 횟수만큼 반복
    - `{n}` : 앞 문자가 n번 반복
    - `{n,}` : 앞 문자 n번 이상 반복
    - `{n,m}` : 앞 문자 n번 이상 m번 이하 반복
    - `?` : 앞 문자 0번 또는 1번
    - `+` : 앞 문자 1번 이상 반복
- 대괄호 `[]` : 어느 한 문자
    - [ABC] 는 A, B, C 중 한 문자 선택
- `[-]` : 지정 범위 중 한 문자
- `^` : 맨 앞, `$` : 맨 뒤
    - “^j.*p$” 는 맨 앞 문자가 j, 맨 뒤 문자가 p인 문자열

```kotlin
println("Kotlin".matches(Regex("K[a-z]+")))
// true
```

---
## 예외 Exception

> 설계자가 의도한대로 사용하지 않으면 터뜨리겠다 — 타당성 검사

- 에러의 종류
    1. 문법 에러 syntax error
    2. 실행 시 에러 runtime error
    3. 논리 에러 logic error

```kotlin
try {
    // 에러가 날 것으로 예상되는 코드 작성
} catch (e: Exception) {
    // e : 에러의 정보를 담고 있는 객체
} finally {
    // 실패하든 성공하든 무조건 실행
    // 데이터베이스에서 ...해제 해줘야하거나 그런 경우에 사용
}
```

- `throw` 문이 있더라도 Kotlin은 `try-catch` 문 자유 / Java는 강제


- `e.printStackTrace()` : 예외가 어디서 터졌는지 출력


- 에러와 예외는 다른것
  - 예외: 로직을 수행하다가 예외가 발생하는 것
  - 에러: 더 큰 개념  
  예외 처리는 runtime error를 대처하는 것


- `Exception`은 `Throwalbe` 를 상속받아 구현됨
  - 에러도 Throwable를 상속받음
  - Throwable은 에러와 예외로 나누어짐

## 파일 조작

- 파일 조작의 기본 순서
    1. 파일을 연다
    2. 파일을 읽거나 쓴다
    3. 파일을 닫는다

```kotlin
// 파일 지정
val file = File("save.txt")     // 경로로 객체 생성

// 내용 쓰기
file.writeText("first text")
file.writeText("Hello world!\nhi!!!")   // 내용 새로쓰기
file.appendText("\nnext text")          // 내용 덧붙이기

// 내용 읽기
val text = file.readText()
println(text)

// 결과:
// Hello world!
// hi!!!
// next text
```

- File.writeText() 덮어쓰기
- File.appendText() 이어쓰기

## 데이터 형식

### CSV

> **Comma-Separated Values**
데이터를 콤마 `,` 로 나눈 형식

```kotlin
val str = "홍길동,한석봉,신사임당"
```



### 프로퍼티 형식의 파일

> Properties 클래스를 사용하여 키(key)와 값(value)의 쌍으로 읽고 쓰기가 가능

```kotlin
// data.properties 파일
heroName=홍길동
heroHp=100
```

### XML

> **Extensible Markup Language**
`<태그>` 를 활용한 기술 방식

- 포함 관계를 기술할 수 있음
- DOM Parser, SAX Parser 등을 통해 파서를 제작할 필요가 있음
    - 자유로워서 paser를 만들어서

```xml
<note>
  <to>Tove</to>
  <from>Jani</from>
  <heading>Reminder</heading>
  <body>Don't forget me this weekend!</body>
</note>
```

### JSON 형식

> **JavaScript Object Notation**  
> `{}`, `[]`, `key: value` 형태

- 사람 입장에서도, 컴퓨터 입장에서도 사용하기 편함
  - 네트워크 통신에서 가장 많이 사용되고 있음
  - XML에 비해 적은 용량, 좋은 가독성

```json
{
  "이름": "홍길동",
  "나이": 25,
  "성별": "여",
  "주소": "서울광역시",
  "특기": ["농구", "도술"],
  "가족관계": {"#": 2, "아버지": "홍판서", "어머니": "춘섬"},
  "회사": "경기 수원시"
}
```

- `[]` 로 리스트, `{}` 로 객체를 표현
- 키(key): 값(value) 형태태

### 직렬화 Serialization

> 데이터 구조나 객체 상태를 저장하고 나중에 재구성할 수 있는 포맷으로 변환하는 과정

- 객체를 파일 형태로 저장하거나, 통신하기 쉬운 포맷으로 변환하는 과정을 의미
  - 클래스 내부의 필드에 다른 클래스가 있다면, 모두 직렬화 처리를 해줘야 함

```text
    메모리 ———(직렬화)——> 물리 저장 공간 
메모리(DB) <—(역직렬화)—— 
```

#### 주로 서버와 JSON으로 통신을 할 것이기 때문에 JSON 직렬화를 기본으로 한다
- 직렬화: 클래스 → JSON
- 역직렬화: JSON → 클래스

```kotlin
// 직렬화 라이브러리 사용을 위한 어노테이션(annotation)
@Serializable
data class User(
    val name: String,
    val email: String,
)
```

```kotlin
// 직렬화
val json = Json.encodeToString(User("홍길동", "aaa@email.com"))
println(json)
// 결과: {"name":"홍길동","email":"aaa@email.com"}
```

```kotlin
// 역직렬화
val obj = Json.decodeFromString<User>(json)
println(obj)
// 결과: User(name=홍길동, email=aaa@email.com)
```
