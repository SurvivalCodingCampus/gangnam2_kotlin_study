package com.luca.kotlinstudy._04_collection

fun main(){
    val a: Double = 0.1
    val b = 0.2
    val c = a + b

    println(c)
    println(c == 0.3)
    /*
    컴퓨터에 소수라는 개념이 없다. 1과 0으로만 구성
    그래서 소수점 계산이 정확도가 제대로 나올 수 없다.
    오차범위를 엡실론이라고 한다.
    그래서 굉장한 작은 값을 정해두고 그 값이랑 비슷하면 그 값이 맞다고 한다.
    C# C C++ 파이썬은 엡실론을 가지고 있다.
     */
}